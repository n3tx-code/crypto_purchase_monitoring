{% extends 'base.html.twig' %}

{% block title %} {{ entity }} not found {% endblock %}

{% block body %}

    <h2 class="text-center mt-4">{{ entity }} not found </h2>

    <div class="row mt-4">
        <div class="col-md-8 offset-md-2">
            Un vrai dresseur pokémon ne se laisse pas faire si facilement. Retourne dans ton pokédex pour visualiser tes
            pokémons.
        </div>
    </div>
    <div class="row mt-4">
        <div class="col-md-8 offset-md-2">
            <a href="{{ path('home') }}" class="btn btn-primary btn-block">Home</a>
        </div>
    </div>

{% endblock %}